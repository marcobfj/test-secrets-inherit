name: test
on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      uses: marcobfj/test-reusable-secrets/.github/workflows/secunday.yaml@main
      with:
        environment: "develop"
        